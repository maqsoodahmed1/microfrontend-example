{
  "name": "react-microfrontends-workspace",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run build:remotes && concurrently \"npm run watch:sms\" \"npm run watch:reports\" \"npm run serve:remotes\" \"npm run dev:parent\"",
    "dev:watch": "concurrently \"npm run watch:sms\" \"npm run watch:reports\" \"npm run dev:parent\"",
    "dev:parent": "cd apps/parent && npm run dev",
    "watch:sms": "cd apps/sms && npm run build -- --watch",
    "watch:reports": "cd apps/reports && npm run build -- --watch",
    "build:remotes": "npm run build:sms && npm run build:reports",
    "serve:remotes": "concurrently \"npm run serve:sms\" \"npm run serve:reports\"",
    "build:sms": "cd apps/sms && npm run build",
    "build:reports": "cd apps/reports && npm run build",
    "serve:sms": "cd apps/sms && npm run preview",
    "serve:reports": "cd apps/reports && npm run preview",
    "build": "npm run build:remotes && cd apps/parent && npm run build"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  }
}